# Chapter 07 Example - GoogleTest from a Local Archive

This example uses a **local GoogleTest archive** instead of downloading GoogleTest from the network.

That makes the chapter easier to run in environments where online dependency fetching is blocked or unreliable.

---

## Expected local setup

Place this file here:

```text
third_party/googletest-1.17.0.tar.gz